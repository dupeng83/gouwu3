<header>
  <h1>商品列表</h1>
</header>

<table>
  <thead>
    <tr>
      <th>商品名</th>
      <th>价格</th>
    </tr>
  </thead>

<% @products.each do |product| %>
  <tr>
    <td><%= link_to product.name, pd_path( product )%></td>
    <td><%= product.price %></td>
  </tr>
<% end %>
</table>
